/**
 * Controls whether the application integrates with Firebase.
 * Set to `true` to enable Firebase authentication, real-time database, and presence.
 * Set to `false` to run the app in a local, offline mode using localStorage for persistence.
 */
export const FIREBASE_ENABLED = false;
